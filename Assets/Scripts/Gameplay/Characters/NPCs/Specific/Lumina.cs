namespace Gameplay.Characters.NPCs.Specific
{
    /// <summary>
    /// Lumina-specific NPC implementation.
    /// Inherits from TalkableNPC to provide type-specific identification in Unity Inspector.
    /// This allows for type-safe references and future Lumina-specific behavior if needed.
    /// </summary>
    public class Lumina : TalkableNPC
    {
        // Currently uses base TalkableNPC functionality.
        // Add Lumina-specific fields/methods here when needed.
        // Example: [SerializeField] private LuminaSpecificData customData;
    }
}
