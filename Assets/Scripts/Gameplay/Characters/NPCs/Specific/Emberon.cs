namespace Gameplay.Characters.NPCs.Specific
{
    /// <summary>
    /// Emberon-specific NPC implementation.
    /// Inherits from TalkableNPC to provide type-specific identification in Unity Inspector.
    /// This allows for type-safe references and future Emberon-specific behavior if needed.
    /// </summary>
    public class Emberon : TalkableNPC
    {
        // Currently uses base TalkableNPC functionality.
        // Add Emberon-specific fields/methods here when needed.
        // Example: [SerializeField] private EmberonSpecificData customData;
    }
}